/* 基础样式不变，新增以下内容 */
/* 字数统计 */
.word-count {
    font-size: 22rpx;
    color: #8baa92;
    text-align: right;
    margin-top: 15rpx;
}

/* 原有样式不变 */
/* 基础样式 */
.container {
    padding: 20rpx;
    min-height: 100vh;
    background-color: #f9fcf9;
}

/* 输入项容器 */
.input-item {
    background-color: white;
    border-radius: 15rpx;
    padding: 25rpx;
    margin-bottom: 25rpx;
    box-shadow: 0 3rpx 10rpx rgba(139, 170, 146, 0.08);
}

/* 标题输入框 */
.title-input {
    font-size: 30rpx;
    font-weight: 600;
    color: #333;
    padding: 10rpx 0;
    border-bottom: 2rpx solid #f0f5f0;
    margin-bottom: 15rpx;
}

/* 正文编辑框 */
.content-input {
    font-size: 26rpx;
    color: #666;
    line-height: 1.8;
    min-height: 400rpx;
}

/* 保存按钮 */
.save-btn {
    width: 100%;
    height: 90rpx;
    line-height: 60rpx;
    background-color: #8baa92;
    color: white;
    font-size: 32rpx;
    border-radius: 15rpx;
    margin-top: 30rpx;
    box-shadow: 0 5rpx 15rpx rgba(139, 170, 146, 0.2);
}

.container {
    padding: 20rpx;
    min-height: 100vh;
    background-color: #f9fcf9;
}

.input-item {
    background-color: white;
    border-radius: 15rpx;
    padding: 25rpx;
    margin-bottom: 25rpx;
    box-shadow: 0 3rpx 10rpx rgba(139, 170, 146, 0.08);
}

/* 其他原有样式不变 */